# Build profile for the hosted portfolio demo:
#   npm run build -- --mode demo
#
# No PHP service is hosted, so client records live in the browser. Payoff
# projections still hit the real Go code, deployed as a serverless function on
# the same origin.
VITE_DEMO_MODE=true
VITE_PAYOFF_API_URL=
